array=[int(x)for x in input().split()]
def minimum(array):
  mini=float('inf')
  for i in range(len(array)):
    if array[i]<mini:
      mini=array[i]
  return mini
print(minimum(array)) 








array=[int(x)for x in input().split()]
def maximum(array):
  maxi=float('-inf')
  for i in range(len(array)):
    if array[i]>maxi:
      maxi=array[i]
  return maxi
print(maximum(array)) 
